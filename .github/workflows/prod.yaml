name: prod pipeline
on: workflow_dispatch

jobs:
  deploy:
     runs-on: ubuntu-latest
     environment: Prod
     steps:
        - name: Printing The Prod env Vars
          run: echo "Env Is: ${{ vars.ENVIRONMENT }}"