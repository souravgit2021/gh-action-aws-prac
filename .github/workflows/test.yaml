name: Test pipeline
on: workflow_dispatch

jobs:
  deploy:
     runs-on: ubuntu-latest
     environment: Test
     steps:
        - name: Printing The Test env Vars
          run: echo "Env Is: ${{ vars.ENVIRONMENT }}"